export const selectIsThemeDropDownOpen = (state) =>
  state.controls.isThemeDropDownOpen;

export const selectIsProfileModalsOpen = (state) =>
  state.controls.isProfileModalOpen;

export const selectSideBarIsOpen = (state) =>
  state.controls.sideBarIsOpen;

export const selectBoardModalIsOpen = (state) =>
  state.controls.isBoardModalOpen;

export const selectIsBoardEdit = (state) =>
  state.controls.isBoardEdit;

export const selectIsColumnEdit = (state) =>
  state.controls.isColumnEdit;

export const selectIsCardEdit = (state) =>
  state.controls.isCardEdit;

export const selectIsFilterModalOpen = (state) =>
  state.controls.isFilterModalOpen;

export const selectIsAddColumnOpen = (state) =>
  state.controls.isAddColumnOpen;