export const selectIsThemeDropDownOpen = (state) =>
  state.controls.isThemeDropDownOpen;

export const selectIsProfileModalsOpen = (state) =>
  state.controls.isProfileModalOpen;

export const selectSideBarIsOpen = (state) => state.controls.sideBarIsOpen;

export const selectBoardModalIsOpen = (state) =>
  state.controls.isBoardModalOpen;

export const selectIsBoardEdit = (state) => state.controls.isBoardEdit;

export const selectIsColumnEdit = (state) => state.controls.isColumnEdit;

export const selectIsCardEdit = (state) => state.controls.isCardEdit;

export const selectIsFilterModalOpen = (state) =>
  state.controls.isFilterModalOpen;

export const selectIsAddColumnOpen = (state) => state.controls.isAddColumnOpen;

export const selectIsAddCardOpen = (state) => state.controls.isAddCardOpen;

export const selectIsProgressOpen = (state) => state.controls.isProgressOpen;

export const selectIsNeedHelpModalOpen = (state) =>
  state.controls.isNeedHelpModalOpen;

export const selectIsConfirmDeleteOpen = (state) => state.controls.isConfirmDeleteOpen;

export const selectIsConfirmBoardDelete = (state) => state.controls.isConfirmBoardDelete;

export const selectIsConfirmColumnDelete = (state) => state.controls.isConfirmColumnDelete;

export const selectIsConfirmCardDelete = (state) => state.controls.isConfirmCardDelete;